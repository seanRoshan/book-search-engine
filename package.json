{
  "name": "book-search-engine",
  "version": "1.0.0",
  "description": "A react application that enable users to search books and store results inside a mongodb database by using graphql",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "Sean Roshan",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
